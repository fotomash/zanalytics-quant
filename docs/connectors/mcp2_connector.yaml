# MCP2 connector configuration template
# Replace the placeholders with deployment-specific values.
server_label: "<server_label>"
server_url: "<server_url>"
require_approval: "never"
tools:
  - search
  - fetch
