# Enrichment Service

This service enriches incoming market data before it is stored or published.

## Running

Run the worker module to consume ticks from Redis and persist enriched
results:

```bash
python -m worker
```

For programmatic use of the enrichment pipeline:

```bash
python -m main
```
